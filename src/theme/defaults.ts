import type { UpdateThemeMetrics } from '../types';

export const defaultTheme: UpdateThemeMetrics = {
  borderRadius: 14,
  spacing: {
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
  },
  typography: {
    titleSize: 20,
    titleLineHeight: 26,
    bodySize: 15,
    bodyLineHeight: 22,
    captionSize: 13,
    captionLineHeight: 18,
    buttonSize: 15,
    buttonLineHeight: 20,
  },
};
